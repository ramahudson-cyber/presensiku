## Plan: Full Premium Redesign — AttendancePage

### Tujuan
Samakan komponen real `AttendancePage` dengan desain `preview_absensi_premium.html`.

### Perubahan per File

#### 1. `AttendancePage.jsx` — Restruktur Layout

**Layout baru:**
- Fullscreen map sebagai background (100% height)
- Server time → mini badge di pojok kiri atas
- Semua info status/lokasi → satu overlay card (seperti preview):
  - Icon pin + "Puskesmas Ampenan" + "Lokasi Absensi"
  - Status pill (green-yellow dot + "Dalam Radius")
  - Distance + shift badge
- Fingerprint button di tengah bawah (80px, ring glow, backdrop-blur)
- Ambient glow orbs (purple top-right, green bottom-left)

**Yang dipertahankan:**
- Semua logic (server time sync, GPS, check-in/out, fake GPS detection, error handling)
- Error/success banners
- AttendanceResultSheet integration

#### 2. `LocationMap.jsx` — Upgrade Visual

- Tile: CartoDB `light_all` (lebih bersih)
- Background: `#0d0a14` (dark purple)
- Add radius circle: 200m, `#ADFF2F`, dashed `6 4`, fill opacity 0.2
- Zoom controls: dark glassmorphic (40x40px, blur(12px), white icons)
- Puskesmas marker: 38px, green-yellow, shadow `0 0 25px rgba(173,255,47,0.5)`
- User marker: 22px, emerald, shadow `0 0 15px rgba(16,185,129,0.6)`
- Distance badge overlay dihapus (pindah ke card)

#### 3. `AttendanceResultSheet.jsx` — Light Mode Premium

- Background putih transparan `rgba(245,245,250,0.96)`
- Teks hitam `#0a0a14`
- Purple accent icons
- Status: Tepat Waktu (purple) / Terlambat (merah)
- Button: gradient purple solid, shadow, hover lift

#### 4. `index.css` — Animasi

- `@keyframes breathe` (dot animasi)
- `@keyframes ring-pulse` (fingerprint ring)
- CSS untuk map dark background `#0d0a14`

### Files yang Diubah
1. `siap-ampenan/src/pages/attendance/AttendancePage.jsx` — layout restructure
2. `siap-ampenan/src/components/LocationMap.jsx` — radius circle, zoom, tiles, dark bg
3. `siap-ampenan/src/components/AttendanceResultSheet.jsx` — sudah, minor tweaks
4. `siap-ampenan/src/index.css` — animasi tambahan
