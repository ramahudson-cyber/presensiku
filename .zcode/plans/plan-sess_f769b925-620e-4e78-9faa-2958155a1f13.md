## Plan: Jarak otomatis pakai meter/kilometer

**File:** `AttendancePage.jsx` — baris 538-540

### 1. Tambah helper function di atas `export default`:
```javascript
function formatDistance(meters) {
  if (meters === null || meters === undefined) return "—";
  if (meters >= 1000) return (meters / 1000).toFixed(1) + " km";
  return Math.round(meters) + " m";
}
```

### 2. Ganti baris 540:
```jsx
// Sebelum:
<strong className="text-black font-bold">{distance || "—"}</strong> dari {puskesmasLocation.name?.toLowerCase?.() || "puskesmas"}

// Sesudah:
<strong className="text-black font-bold">{formatDistance(distance)}</strong> dari {puskesmasLocation.name || "puskesmas"}
```

### Hasil:
| Jarak | Tampil |
|-------|--------|
| 18 m | **18 m dari Puskesmas Ampenan** |
| 1500 m | **1.5 km dari Puskesmas Ampenan** |

Satuan menyesuaikan jarak, user langsung paham. Commit & push.