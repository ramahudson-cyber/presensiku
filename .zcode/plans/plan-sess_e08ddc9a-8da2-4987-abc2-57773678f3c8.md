## Implementasi Month/Year Picker

### File: `src/pages/employee/EmployeeSchedule.jsx`

**1. Tambah import:** `BottomSheet` dari `../../components/BottomSheet`

**2. Tambah state:**
- `showMonthPicker` (bool) — kontrol visibilitas sheet
- `pickerYear` (int) — tahun yang lagi dibrowse di picker

**3. Ganti nav bar** (lines 199-207):
- Hapus tombol `ChevronLeft` / `ChevronRight` + span text
- Ganti jadi **1 tombol** dengan icon Calendar + teks "Juli 2026" + ChevronDown
- onClick → `openMonthPicker()` (set pickerYear = year, buka sheet)

**4. BottomSheet** `showMonthPicker`:
- **Title:** "Pilih Bulan"
- **Subtitle:** "Tahun {pickerYear}"
- **Year nav:** 2 tombol (prev/next year) + angka tahun
- **Month grid:** `grid-cols-4 gap-2` — 12 bulan, bulan aktif di-highlight `bg-electric-violet text-white`
- Klik bulan → `selectMonth(i)` (setMonth, setYear, tutup sheet)

**5. Tombol "Hari Ini"** (line 208-213) dan **stats counter** (line 214-216) tetap.