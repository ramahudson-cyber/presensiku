## Plan: Zoom [+][−] di pojok kanan bawah overlay

### 1. `LocationMap.jsx` — Expose map instance via callback
- Destructure `onMapReady` from props
- Panggil `onMapReady?.(map)` setelah `mapInstanceRef.current = map` (line 69)

### 2. `AttendancePage.jsx` — Zoom buttons bottom-right
- Tambah `Plus, Minus` ke import lucide-react (baris 5)
- Tambah ref: `const zoomMapRef = useRef(null)`
- Pass `onMapReady={(m) => zoomMapRef.current = m}` ke `<LocationMap>`
- Tambah div absolute di pojok kanan bawah overlay:
  ```jsx
  <div className="absolute bottom-28 right-4 flex flex-col gap-2 pointer-events-auto z-20">
    <button onClick={() => zoomMapRef.current?.zoomIn()}
      className="w-9 h-9 rounded-xl bg-white/80 backdrop-blur-xl border border-black/10 flex items-center justify-center shadow-sm hover:bg-white transition-all">
      <Plus size={16} className="text-black" />
    </button>
    <button onClick={() => zoomMapRef.current?.zoomOut()}
      className="w-9 h-9 rounded-xl bg-white/80 backdrop-blur-xl border border-black/10 flex items-center justify-center shadow-sm hover:bg-white transition-all">
      <Minus size={16} className="text-black" />
    </button>
  </div>
  ```

Hanya 2 file diubah.