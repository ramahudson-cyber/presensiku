## Plan: Fix Attendance Result Sheet — Premium Preview

### Goal
Bottom sheet (modal-card) background ungu-magenta + hitam yang jelas, dan confetti (kertas-kertas kecil warna-warni) jatuh terus-menerus dari atas ke bawah di belakangnya.

### Changes

#### File: `attendance-result-premium-preview.html`

**1. CSS: Confetti animation — ganti static jadi jatuh infinite**
- Ganti `@keyframes confetti-fall` → `translateY(-120px)` → `translateY(820px)` + `rotate(720deg)` + sway X
- Tambah class `.cf-1` s/d `.cf-80` dengan `animation: confetti-fall linear infinite` + `animation-delay` berbeda (0s–2.9s) dan `animation-duration` acak (2.5s–3.9s)
- `z-index: 1` agar confetti di belakang modal-card
- Remove `confetti-spread` keyframes (tidak dipakai lagi)

**2. CSS: Modal card background**
- Pastikan gradient ungu-magenta+hitam benar: `linear-gradient(165deg, #8B00CC 0%, #7B00E0 20%, #4A0099 50%, #05020b 100%)`
- Border: `rgba(255,0,153,0.35)`

**3. CSS: 3D Badge — pastikan gradient tampil**
- `background: linear-gradient(145deg, #FF0099 0%, #BF00FF 50%, #7B00E0 100%)`
- Late badge: `linear-gradient(145deg, #FBBF24 0%, #F59E0B 50%, #D97706 100%)`

**4. HTML: 80 confetti particles**
- Tambah 80 `<span>` dengan class `cf-1`–`cf-80` dan warna `c1`–`c6`
- Posisi left acak 0–100%, ukuran 3px–6px, warna berbeda

### Expected result
- Confetti jatuh terus-menerus dari atas dengan rotasi, warna-warni, di belakang bottom sheet
- Bottom sheet background ungu-magenta gelap ke hitam, gradient jelas terlihat